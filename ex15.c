#include <stdio.h>

int main() {
  int n, j, m, cont;
  printf("Entre com um número positivo n: \n");
  scanf("%d", &n);
  printf("Entre com um número positivo j: \n");
  scanf("%d", &j);
  printf("Entre com um número positivo m: \n");
  scanf("%d", &m);
  for () {
    if ( % m == j % m) {

    }
  }
  return 0;
}